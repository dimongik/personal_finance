#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка = Истина Тогда
		Возврат;
	КонеЦЕсли;
	
	ИмяДляРазработчика = Строка(Новый УникальныйИдентификатор());
	ИмяДляРазработчика = СтрЗаменить(ИмяДляРазработчика,"-","");
	
КонецПроцедуры


#КонецОбласти